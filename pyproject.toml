[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "avaya-case-review-runtime"
version = "1.10.1"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["avaya_case_review_runtime"]
